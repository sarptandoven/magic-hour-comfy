# magic-hour-comfy

Magic Hour Comfy custom nodes and workflow utilities for AI video/image generation experiments.

## Overview

This repository contains source code and workflow files for `magic-hour-comfy`.

## Local setup

Install project dependencies in a local virtual environment and keep generated caches, local environment files, and credentials out of Git.
